# gcovr breaks when it cannot find a common path.
--- a/scripts/gcovr
+++ b/scripts/gcovr
@@ -418,6 +418,7 @@
                 break
             else:
                 return ''
+        if not common_dirs or (len(common_dirs) == 1 and not common_dirs[0]): return ''
 
     return os.path.join( os.path.relpath( os.path.sep.join( common_dirs ) ), '' )
 
